{
  "name": "one-deal-contracts",
  "private": true,
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:flare": "hardhat run scripts/deploy.ts --network flare",
    "deploy:songbird": "hardhat run scripts/deploy.ts --network songbird"
  },
  "dependencies": {
    "@nomicfoundation/hardhat-toolbox": "^6.1.2",
    "@openzeppelin/contracts": "^5.6.1",
    "dotenv": "^17.3.1",
    "hardhat": "^2.28.6"
  }
}
